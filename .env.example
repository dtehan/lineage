# Lineage Application Environment Configuration
# Copy this file to .env and update with your values
# Environment variables take precedence over .env file values

# =============================================================================
# Teradata Database Connection
# =============================================================================
# Used by: Python scripts (database/), Python server, Go server

# For Python database scripts (database/*.py)
TD_HOST=your-teradata-host.example.com
TD_USER=your_username
TD_PASSWORD=your_password
TD_DATABASE=demo_user

# For Go server and Python Flask server (lineage-api/)
# These take precedence over TD_* if both are set
TERADATA_HOST=your-teradata-host.example.com
TERADATA_USER=your_username
TERADATA_PASSWORD=your_password
TERADATA_DATABASE=demo_user
TERADATA_PORT=1025

# =============================================================================
# Redis Cache (Optional)
# =============================================================================
# Used by: Go server
# If Redis is unavailable, the application falls back gracefully

REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# =============================================================================
# Server Configuration
# =============================================================================
# Used by: Go server

PORT=8080

# =============================================================================
# ClearScape Analytics Test Environment (for development/testing)
# =============================================================================
# Uncomment these lines to use the ClearScape Analytics test database:
#
# TD_HOST=test-sad3sstx4u4llczi.env.clearscape.teradata.com
# TD_USER=demo_user
# TD_PASSWORD=password
# TD_DATABASE=demo_user
#
# TERADATA_HOST=test-sad3sstx4u4llczi.env.clearscape.teradata.com
# TERADATA_USER=demo_user
# TERADATA_PASSWORD=password
# TERADATA_DATABASE=demo_user
