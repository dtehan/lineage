# config.yaml - Data Lineage API Configuration
port: 8080

teradata:
  host: ${TERADATA_HOST}
  port: 1025
  user: ${TERADATA_USER}
  password: ${TERADATA_PASSWORD}
  database: lineage

redis:
  addr: ${REDIS_ADDR:localhost:6379}
  password: ${REDIS_PASSWORD:}
  db: 0
