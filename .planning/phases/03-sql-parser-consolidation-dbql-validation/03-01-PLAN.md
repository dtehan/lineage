---
phase: 03-sql-parser-consolidation-dbql-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lineage-api/utils/sql_parser.py
  - database/scripts/populate/dbql_extractor.py
  - database/archive/sql_parser.py
  - database/scripts/populate/sql_parser.py
autonomous: true

must_haves:
  truths:
    - "sql_parser.py exists in lineage-api/utils/ and is importable from there"
    - "No duplicate sql_parser.py exists in database/archive/"
    - "No sql_parser.py remains in database/scripts/populate/"
    - "dbql_extractor.py imports TeradataSQLParser from lineage-api/utils/sql_parser"
    - "Python can import TeradataSQLParser from the new location without error"
  artifacts:
    - path: "lineage-api/utils/sql_parser.py"
      provides: "Canonical SQL parser module (TeradataSQLParser class)"
      min_lines: 680
    - path: "database/scripts/populate/dbql_extractor.py"
      provides: "Updated import referencing lineage-api/utils/sql_parser"
      contains: "from utils.sql_parser import TeradataSQLParser"
  key_links:
    - from: "database/scripts/populate/dbql_extractor.py"
      to: "lineage-api/utils/sql_parser.py"
      via: "sys.path + absolute import"
      pattern: "from utils\\.sql_parser import TeradataSQLParser"
---

<objective>
Consolidate the two identical sql_parser.py files into a single canonical location at lineage-api/utils/sql_parser.py and update all import references.

Purpose: Eliminate duplicate code that can drift out of sync and clarify the canonical location for SQL parsing logic (CLEANUP-01, CLEANUP-02, CLEANUP-03).
Output: Single sql_parser.py in lineage-api/utils/, both duplicates removed, all imports updated.
</objective>

<execution_context>
@/Users/Daniel.Tehan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Daniel.Tehan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sql-parser-consolidation-dbql-validation/03-RESEARCH.md
@database/scripts/populate/sql_parser.py
@database/scripts/populate/dbql_extractor.py
@lineage-api/utils/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move sql_parser.py to lineage-api/utils/ and delete duplicates</name>
  <files>
    lineage-api/utils/sql_parser.py
    database/archive/sql_parser.py
    database/scripts/populate/sql_parser.py
  </files>
  <action>
1. First, verify the two files are identical: `diff database/archive/sql_parser.py database/scripts/populate/sql_parser.py` (should produce no output).

2. Copy `database/scripts/populate/sql_parser.py` to `lineage-api/utils/sql_parser.py`. This is the active copy (same content as archive).

3. Update the module docstring's Usage example in the new file. Change `from sql_parser import TeradataSQLParser` to `from utils.sql_parser import TeradataSQLParser` to reflect the new import path.

4. Delete `database/archive/sql_parser.py` (the duplicate in archive/).

5. Delete `database/scripts/populate/sql_parser.py` (the original, now moved to lineage-api/utils/).

6. Verify `lineage-api/utils/sql_parser.py` exists and has ~684 lines.
  </action>
  <verify>
- `ls -la lineage-api/utils/sql_parser.py` shows the file exists with ~25KB size
- `test ! -f database/archive/sql_parser.py` exits 0 (file deleted)
- `test ! -f database/scripts/populate/sql_parser.py` exits 0 (file deleted)
- `wc -l lineage-api/utils/sql_parser.py` shows ~684 lines
  </verify>
  <done>sql_parser.py exists only in lineage-api/utils/, both duplicates deleted</done>
</task>

<task type="auto">
  <name>Task 2: Update dbql_extractor.py import to reference new parser location</name>
  <files>
    database/scripts/populate/dbql_extractor.py
  </files>
  <action>
1. In `database/scripts/populate/dbql_extractor.py`, the current import block (lines 43-48) is:
```python
try:
    from sql_parser import TeradataSQLParser
except ImportError:
    from scripts.populate.sql_parser import TeradataSQLParser
```

2. Replace this entire try/except import block with a clean absolute import that uses the existing sys.path setup (line 31 already adds project root to sys.path):
```python
# Import SQL parser from canonical location (lineage-api/utils/)
sys.path.insert(0, str(Path(__file__).parent.parent.parent / "lineage-api"))
from utils.sql_parser import TeradataSQLParser
```

Note: Line 31 already has `sys.path.insert(0, str(Path(__file__).parent.parent.parent))` which adds the project root. We need to also add the lineage-api directory to sys.path so `from utils.sql_parser import ...` resolves correctly. Add the second sys.path.insert right after the existing one (near line 31), then replace the old try/except import block with the clean import.

3. Verify the import works by running: `cd /path/to/project && python -c "import sys; from pathlib import Path; sys.path.insert(0, str(Path('lineage-api'))); from utils.sql_parser import TeradataSQLParser; print('Import OK:', TeradataSQLParser.__name__)"`

4. Also check `database/archive/extract_dbql_lineage.py` (archived file, lines 180-184). This file has a similar import. Since this is in the archive directory and is itself an archived file, leave it as-is -- it is not actively used and the archive is for historical reference only.
  </action>
  <verify>
- `cd /path/to/project && python -c "import sys; from pathlib import Path; sys.path.insert(0, str(Path('lineage-api'))); from utils.sql_parser import TeradataSQLParser; print('OK')"` prints "OK"
- `grep -n "from utils.sql_parser import" database/scripts/populate/dbql_extractor.py` shows the new import
- `grep -c "from sql_parser import" database/scripts/populate/dbql_extractor.py` returns 0 (old import gone)
  </verify>
  <done>dbql_extractor.py imports TeradataSQLParser from lineage-api/utils/sql_parser.py via clean absolute import, no fallback try/except needed</done>
</task>

</tasks>

<verification>
1. Only one sql_parser.py exists in the entire project (in lineage-api/utils/)
2. `grep -r "from sql_parser import" database/scripts/populate/` returns no results from active scripts
3. Python import verification succeeds from project root
4. No import errors when loading dbql_extractor module
</verification>

<success_criteria>
- sql_parser.py exists at lineage-api/utils/sql_parser.py only (CLEANUP-01)
- database/archive/sql_parser.py deleted (CLEANUP-02)
- database/scripts/populate/sql_parser.py deleted (consolidation complete)
- dbql_extractor.py references the new location (CLEANUP-03)
- Import verification passes without error
</success_criteria>

<output>
After completion, create `.planning/phases/03-sql-parser-consolidation-dbql-validation/03-01-SUMMARY.md`
</output>
