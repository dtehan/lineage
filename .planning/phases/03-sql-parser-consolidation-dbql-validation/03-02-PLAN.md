---
phase: 03-sql-parser-consolidation-dbql-validation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - database/scripts/populate/dbql_extractor.py
  - database/scripts/validate_migration.py
autonomous: true

must_haves:
  truths:
    - "DBQL extraction logs a warning when SQL text exceeds 32000 characters"
    - "A regression validation script exists that captures and compares OL_COLUMN_LINEAGE record counts"
    - "Validation script can be run before and after consolidation to prove data integrity"
  artifacts:
    - path: "database/scripts/populate/dbql_extractor.py"
      provides: "Truncation warning logging for oversized DBQL SQL text"
      contains: "truncat"
    - path: "database/scripts/validate_migration.py"
      provides: "Regression validation script comparing record counts and sample hashes"
      min_lines: 50
      exports: ["capture_baseline", "validate_migration"]
  key_links:
    - from: "database/scripts/validate_migration.py"
      to: "OL_COLUMN_LINEAGE table"
      via: "SQL COUNT query"
      pattern: "SELECT COUNT.*OL_COLUMN_LINEAGE"
    - from: "database/scripts/populate/dbql_extractor.py"
      to: "loguru or logging"
      via: "logger.warning for truncation"
      pattern: "logger\\.warning.*truncat"
---

<objective>
Add DBQL SQL text truncation warnings and create a regression validation script to verify lineage data integrity after parser consolidation.

Purpose: Ensure DBQL extraction handles oversized SQL gracefully with visible warnings (CLEANUP-04 tooling), and provide a runnable script to prove record counts are unchanged after consolidation.
Output: Enhanced dbql_extractor.py with truncation logging, standalone validation script.
</objective>

<execution_context>
@/Users/Daniel.Tehan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Daniel.Tehan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sql-parser-consolidation-dbql-validation/03-RESEARCH.md
@.planning/phases/03-sql-parser-consolidation-dbql-validation/03-01-SUMMARY.md
@database/scripts/populate/dbql_extractor.py
@database/db_config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DBQL SQL text truncation warning logging</name>
  <files>
    database/scripts/populate/dbql_extractor.py
  </files>
  <action>
In the `fetch_queries` method of `DBQLExtractor` class in `database/scripts/populate/dbql_extractor.py`, the SQL queries cast `SQLTextInfo` CLOB to `VARCHAR(32000)` (lines 204 and 224). Queries longer than 32000 characters are silently truncated.

Add truncation detection and warning:

1. In both SQL queries within `fetch_queries()` (the `since` branch and the `else` branch), add `LENGTH(s.SQLTextInfo)` as an additional SELECT column named `sql_length`:
```sql
CAST(s.SQLTextInfo AS VARCHAR(32000)) as query_text,
LENGTH(s.SQLTextInfo) as sql_length,
```

2. Update the `fetch_queries` return to include `sql_length`. The tuples change from 5 elements to 6: `(QueryID, StatementType, query_text, StartTime, DefaultDatabase, sql_length)`.

3. After fetching results, add a warning log for any queries where `sql_length > 32000`:
```python
queries = self.cursor.fetchall()
# Warn about truncated SQL text
truncated_count = sum(1 for q in queries if q[5] and q[5] > 32000)
if truncated_count:
    logger.warning(
        "DBQL truncation: %d of %d queries exceed VARCHAR(32000) limit - "
        "lineage extraction may be incomplete for these queries",
        truncated_count, len(queries)
    )
```

4. In the `extract_lineage` method's processing loop (line 292), update the tuple unpacking to include `sql_length`:
```python
for i, (query_id, stmt_type, query_text, query_time, default_db, sql_length) in enumerate(queries):
```

5. Add a per-query warning when processing a truncated query (inside the loop, before parsing):
```python
if sql_length and sql_length > 32000:
    logger.warning(
        "Query %s SQL text truncated: %d chars (limit: 32000) - "
        "target: %s",
        query_id, sql_length, self._extract_target_table(query_text)
    )
```

This ensures users see clear warnings in extraction logs when SQL truncation occurs, matching the research recommendation.
  </action>
  <verify>
- `grep -n "truncat" database/scripts/populate/dbql_extractor.py` shows warning log lines
- `grep -n "sql_length" database/scripts/populate/dbql_extractor.py` shows the new column
- `grep -n "LENGTH(s.SQLTextInfo)" database/scripts/populate/dbql_extractor.py` shows LENGTH in both SQL queries
- Python syntax check: `python -c "import ast; ast.parse(open('database/scripts/populate/dbql_extractor.py').read()); print('Syntax OK')"`
  </verify>
  <done>dbql_extractor.py logs warnings when SQL text exceeds VARCHAR(32000) limit during DBQL extraction</done>
</task>

<task type="auto">
  <name>Task 2: Create regression validation script for lineage data integrity</name>
  <files>
    database/scripts/validate_migration.py
  </files>
  <action>
Create `database/scripts/validate_migration.py` -- a standalone script that captures OL_COLUMN_LINEAGE record counts and sample hashes before/after the parser consolidation to prove CLEANUP-04 (regression validation).

The script should:

1. Use the existing `database/db_config.py` for connection configuration (import pattern: `sys.path.insert(0, str(Path(__file__).parent.parent))` then `from db_config import CONFIG`).

2. Implement two modes via CLI arguments:
   - `--capture baseline.json`: Capture current OL_COLUMN_LINEAGE state to JSON file
   - `--validate baseline.json`: Compare current state against saved baseline

3. `capture_baseline(cursor, database)` function:
   - Query `SELECT COUNT(*) FROM {database}.OL_COLUMN_LINEAGE WHERE is_active = 'Y'` for total count
   - Query sample of records (first 100 by lineage_id) with key columns: `lineage_id, source_namespace, source_dataset, source_field, target_namespace, target_dataset, target_field, transformation_type`
   - Hash each sample record with hashlib.sha256
   - Return dict: `{"total_count": int, "sample_hashes": list, "timestamp": str}`

4. `validate_migration(cursor, database, baseline)` function:
   - Capture current state using same capture_baseline function
   - Compare total_count (must match exactly)
   - Compare sample_hashes (must match exactly)
   - Print clear PASS/FAIL result with details

5. Main block:
   - Parse args with argparse (--capture FILE, --validate FILE, --database DB)
   - Connect to Teradata using `teradatasql` with CONFIG values
   - Run capture or validate mode
   - Exit 0 on success, exit 1 on failure

6. Add module docstring explaining usage:
```
Usage:
  # Before consolidation
  python scripts/validate_migration.py --capture baseline.json

  # After consolidation and re-population
  python scripts/validate_migration.py --validate baseline.json
```

Use the research pattern from 03-RESEARCH.md as a guide, but adapt to use existing db_config.py connection pattern rather than raw credentials.
  </action>
  <verify>
- `python -c "import ast; ast.parse(open('database/scripts/validate_migration.py').read()); print('Syntax OK')"`
- `python database/scripts/validate_migration.py --help` shows usage with --capture and --validate options
- `wc -l database/scripts/validate_migration.py` shows 80+ lines
  </verify>
  <done>Standalone validation script exists that can capture and compare OL_COLUMN_LINEAGE state before/after consolidation, supporting CLEANUP-04 regression validation</done>
</task>

</tasks>

<verification>
1. `dbql_extractor.py` has truncation warning logging with sql_length detection
2. `validate_migration.py` runs with --help and shows capture/validate modes
3. Both files pass Python syntax validation
4. No import errors in either file
</verification>

<success_criteria>
- DBQL extraction warns when SQL text exceeds 32000 chars (truncation visibility)
- Regression validation script provides capture and validate modes (CLEANUP-04 tooling)
- Both scripts pass syntax checks and are runnable
</success_criteria>

<output>
After completion, create `.planning/phases/03-sql-parser-consolidation-dbql-validation/03-02-SUMMARY.md`
</output>
