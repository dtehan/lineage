---
phase: 01-foundation-refactoring-impact-analysis-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lineage-api/repositories/__init__.py
  - lineage-api/repositories/base.py
  - lineage-api/repositories/lineage_repository.py
  - lineage-api/repositories/dataset_repository.py
  - lineage-api/config.py
autonomous: true

must_haves:
  truths:
    - "Recursive CTE for upstream lineage exists in exactly one function"
    - "Recursive CTE for downstream lineage exists in exactly one function"
    - "Database lineage CTE exists in exactly one function"
    - "Dataset/field/namespace queries exist in repository functions, not inline in routes"
    - "All repository functions accept a connection parameter and return typed dicts"
  artifacts:
    - path: "lineage-api/repositories/lineage_repository.py"
      provides: "Shared recursive CTE functions for upstream, downstream, and database lineage"
      exports: ["LineageRepository"]
    - path: "lineage-api/repositories/dataset_repository.py"
      provides: "Dataset, field, namespace, search, statistics, and DDL query functions"
      exports: ["DatasetRepository"]
    - path: "lineage-api/repositories/base.py"
      provides: "Base repository class with connection management"
      exports: ["BaseRepository"]
    - path: "lineage-api/repositories/__init__.py"
      provides: "Package exports"
    - path: "lineage-api/config.py"
      provides: "Centralized database and server configuration"
      exports: ["DB_CONFIG", "get_db_connection"]
  key_links:
    - from: "lineage-api/repositories/lineage_repository.py"
      to: "lineage-api/repositories/base.py"
      via: "class inheritance"
      pattern: "class LineageRepository\\(BaseRepository\\)"
    - from: "lineage-api/repositories/dataset_repository.py"
      to: "lineage-api/repositories/base.py"
      via: "class inheritance"
      pattern: "class DatasetRepository\\(BaseRepository\\)"
    - from: "lineage-api/repositories/lineage_repository.py"
      to: "OL_COLUMN_LINEAGE"
      via: "recursive CTE SQL"
      pattern: "WITH RECURSIVE.*FROM OL_COLUMN_LINEAGE"
---

<objective>
Extract repository layer from monolithic python_server.py

Purpose: The current 1455-line python_server.py has 5 duplicate recursive CTE queries across 3 lineage endpoints, with all database queries inline in route handlers. This plan extracts all SQL queries into a repository layer with parameterized functions, creating the foundation that all subsequent plans depend on.

Output: lineage-api/repositories/ package with LineageRepository and DatasetRepository classes, plus lineage-api/config.py for centralized configuration.
</objective>

<execution_context>
@/Users/Daniel.Tehan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Daniel.Tehan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-refactoring-impact-analysis-core/01-RESEARCH.md
@lineage-api/python_server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config.py and base repository</name>
  <files>lineage-api/config.py, lineage-api/repositories/__init__.py, lineage-api/repositories/base.py</files>
  <action>
Create lineage-api/config.py by extracting the configuration logic from python_server.py (lines 22-98):
- Move the dotenv loading logic
- Move REQUIRED_CREDENTIALS and validate_required_credentials()
- Move DB_CONFIG dictionary
- Move get_db_connection() function
- Export DB_CONFIG, get_db_connection, validate_required_credentials
- Call validate_required_credentials() at module load time (same as current behavior)

Create lineage-api/repositories/__init__.py with imports of LineageRepository and DatasetRepository (these files will be created in Task 2).

Create lineage-api/repositories/base.py with BaseRepository class:
- Constructor accepts a teradatasql connection object
- Stores connection as self.connection
- Provides a helper method _strip(value) that returns value.strip() if string, else value (to eliminate the repeated `row[N].strip() if row[N] else ""` pattern throughout python_server.py)
- Provides a helper method _isoformat(value) that returns value.isoformat() if datetime, else None

Do NOT modify python_server.py in this task. The existing server must continue working unchanged.
  </action>
  <verify>
Run `python -c "from config import DB_CONFIG, get_db_connection; print('config OK')"` from lineage-api/ directory.
Run `python -c "from repositories.base import BaseRepository; print('base OK')"` from lineage-api/ directory.
  </verify>
  <done>config.py exports DB_CONFIG and get_db_connection. BaseRepository class exists with connection management and helper methods. All imports succeed without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Extract LineageRepository and DatasetRepository</name>
  <files>lineage-api/repositories/lineage_repository.py, lineage-api/repositories/dataset_repository.py</files>
  <action>
Create lineage-api/repositories/lineage_repository.py with class LineageRepository(BaseRepository):

1. get_upstream_lineage(dataset_name: str, field_name: str, max_depth: int = 5) -> list[dict]:
   Extract the upstream recursive CTE from python_server.py lines 716-761. This EXACT CTE is duplicated in get_openlineage_lineage() (line 716) and get_openlineage_table_lineage() (line 985). Include the `depth` column in SELECT DISTINCT output (currently omitted in python_server.py but needed for impact analysis). Return list of dicts with keys: source_namespace, source_dataset, source_field, target_namespace, target_dataset, target_field, transformation_type, depth.

2. get_downstream_lineage(dataset_name: str, field_name: str, max_depth: int = 5) -> list[dict]:
   Extract the downstream recursive CTE from python_server.py lines 804-850. Same duplication pattern. Include depth in output. Return same dict shape as upstream.

3. get_database_lineage(dataset_names: list[str], max_depth: int = 3) -> list[dict]:
   Extract the database-level recursive CTE from python_server.py lines 1261-1310. This takes a list of dataset names and returns all lineage relationships involving those datasets. Return same dict shape plus depth.

Important implementation notes from research:
- Use TRIM() on dataset/field names in CTE join conditions (current code does NOT trim, which is a known pitfall for Teradata CHAR columns)
- Keep CAST(... AS VARCHAR(10000)) for path column
- Keep POSITION()-based cycle detection
- Keep is_active = 'Y' filter
- Depth column: include `depth` in SELECT DISTINCT (the current code selects it in the CTE but drops it in the final SELECT)
- Use self.connection and self._strip() helper from BaseRepository

Create lineage-api/repositories/dataset_repository.py with class DatasetRepository(BaseRepository):

1. get_namespace(namespace_id: str) -> dict | None: Extract from lines 150-172
2. list_namespaces() -> list[dict]: Extract from lines 113-138
3. get_dataset(dataset_id: str) -> dict | None: Extract from lines 248-309 (includes fields)
4. list_datasets(namespace_id: str, limit: int = 100, offset: int = 0) -> tuple[list[dict], int]: Extract from lines 180-240 (returns datasets and total count)
5. search_datasets(query: str, limit: int = 50) -> list[dict]: Extract from lines 550-593
6. unified_search(query: str, limit: int = 50) -> dict: Extract from lines 606-680
7. get_dataset_statistics(dataset_id: str) -> dict | None: Extract from lines 318-416
8. get_dataset_ddl(dataset_id: str) -> dict | None: Extract from lines 423-541
9. get_dataset_name(dataset_id: str) -> str | None: Simple helper that queries OL_DATASET by dataset_id and returns the name
10. get_dataset_fields(dataset_id: str) -> list[str]: Returns field names for a dataset from OL_DATASET_FIELD
11. get_dataset_with_namespace(dataset_id: str) -> dict | None: Returns dataset name + namespace_uri (used by lineage endpoints)
12. get_field_metadata(dataset_name: str, field_name: str) -> dict | None: Returns field_type and nullable for a field (used by database lineage)
13. get_dataset_metadata(dataset_name: str) -> dict | None: Returns source_type and namespace_uri for a dataset (used by database lineage for external datasets)

For all methods:
- Use self._strip() and self._isoformat() helpers from BaseRepository
- Return dict shapes that match the current JSON response format (camelCase keys like "sourceType", "createdAt")
- Return None for not-found cases (don't raise exceptions - that's for the service layer in Plan 02)
- Use parameterized queries (?) for all user input - never f-strings for SQL

Do NOT modify python_server.py. The existing server must continue working unchanged.
  </action>
  <verify>
Run `python -c "from repositories.lineage_repository import LineageRepository; print('lineage repo OK')"` from lineage-api/ directory.
Run `python -c "from repositories.dataset_repository import DatasetRepository; print('dataset repo OK')"` from lineage-api/ directory.
Run `python -c "from repositories import LineageRepository, DatasetRepository; print('package OK')"` from lineage-api/ directory.
  </verify>
  <done>LineageRepository has 3 methods (get_upstream_lineage, get_downstream_lineage, get_database_lineage) with deduplicated CTE SQL. DatasetRepository has 13 methods covering all current query patterns. All imports succeed. python_server.py is unchanged and still works.</done>
</task>

</tasks>

<verification>
1. All Python imports in lineage-api/repositories/ succeed without errors
2. python_server.py is completely unchanged (git diff shows no modifications)
3. config.py loads .env and exports get_db_connection correctly
4. LineageRepository contains exactly 3 CTE methods (not 5+ duplicates)
5. No f-string SQL in any repository file (all use parameterized ?)
</verification>

<success_criteria>
- Repository layer extracted as standalone Python package
- Zero changes to python_server.py (backward compatibility preserved)
- Recursive CTE SQL exists in exactly one place per direction (upstream/downstream/database)
- All repository classes importable without runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-refactoring-impact-analysis-core/01-01-SUMMARY.md`
</output>
