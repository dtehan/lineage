---
phase: 01-foundation-refactoring-impact-analysis-core
plan: 06
type: execute
wave: 4
depends_on: ["01-04", "01-05"]
files_modified:
  - lineage-ui/src/components/domain/ImpactAnalysis/ImpactTable.test.tsx
  - lineage-ui/src/components/domain/ImpactAnalysis/ImpactSummary.test.tsx
  - lineage-ui/src/api/hooks/useImpact.test.tsx
autonomous: false

must_haves:
  truths:
    - "Frontend unit tests verify ImpactTable renders columns with correct data"
    - "Frontend unit tests verify ImpactSummary displays correct counts"
    - "Frontend unit tests verify useImpactAnalysis hook calls correct API endpoint"
    - "All existing frontend tests still pass (260+ tests)"
    - "Frontend build succeeds with all new components"
    - "User visually confirms impact analysis page works end-to-end"
  artifacts:
    - path: "lineage-ui/src/components/domain/ImpactAnalysis/ImpactTable.test.tsx"
      provides: "Unit tests for TanStack Table impact table"
    - path: "lineage-ui/src/components/domain/ImpactAnalysis/ImpactSummary.test.tsx"
      provides: "Unit tests for summary cards"
    - path: "lineage-ui/src/api/hooks/useImpact.test.tsx"
      provides: "Unit tests for impact analysis hook"
  key_links:
    - from: "lineage-ui/src/components/domain/ImpactAnalysis/ImpactTable.test.tsx"
      to: "lineage-ui/src/components/domain/ImpactAnalysis/ImpactTable.tsx"
      via: "component render and assertion"
      pattern: "render\\(<ImpactTable"
    - from: "lineage-ui/src/api/hooks/useImpact.test.tsx"
      to: "lineage-ui/src/api/hooks/useImpact.ts"
      via: "hook testing"
      pattern: "useImpactAnalysis"
---

<objective>
Add frontend unit tests and perform end-to-end verification

Purpose: Validate the complete Phase 1 implementation with unit tests for new Impact Analysis components and a human verification checkpoint to confirm the full stack works. This plan ensures all existing tests still pass (no regressions) and the new feature is covered.

Output: Unit test files for Impact components, confirmation that all test suites pass, human-verified end-to-end functionality.
</objective>

<execution_context>
@/Users/Daniel.Tehan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Daniel.Tehan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-refactoring-impact-analysis-core/01-04-SUMMARY.md
@.planning/phases/01-foundation-refactoring-impact-analysis-core/01-05-SUMMARY.md
@lineage-ui/src/components/domain/ImpactAnalysis/ImpactTable.tsx
@lineage-ui/src/components/domain/ImpactAnalysis/ImpactSummary.tsx
@lineage-ui/src/api/hooks/useImpact.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write unit tests for Impact Analysis components</name>
  <files>lineage-ui/src/components/domain/ImpactAnalysis/ImpactTable.test.tsx, lineage-ui/src/components/domain/ImpactAnalysis/ImpactSummary.test.tsx, lineage-ui/src/api/hooks/useImpact.test.tsx</files>
  <action>
Create lineage-ui/src/components/domain/ImpactAnalysis/ImpactTable.test.tsx:
Follow the existing test patterns from the codebase (describe/it blocks, React Testing Library).

Test cases:
1. "renders table headers (Database, Table, Column, Depth, Impact Type)"
2. "renders impact data rows correctly"
3. "displays depth badges with correct styling" (depth 1 = blue, depth 2 = amber)
4. "displays impact type badges (direct = red, indirect = amber)"
5. "shows empty message when data is empty array"
6. "renders correct number of rows matching data length"
7. "columns are sortable (clicking header toggles sort)"

Test data fixture:
```typescript
const mockImpactAssets: ImpactAsset[] = [
  { databaseName: 'demo_user', tableName: 'STG_SALES', columnName: 'sale_amount', depth: 1, impactType: 'direct' },
  { databaseName: 'demo_user', tableName: 'DIM_PRODUCT', columnName: 'price', depth: 2, impactType: 'indirect' },
  { databaseName: 'analytics', tableName: 'RPT_REVENUE', columnName: 'total_sales', depth: 3, impactType: 'indirect' },
];
```

Create lineage-ui/src/components/domain/ImpactAnalysis/ImpactSummary.test.tsx:
Test cases:
1. "renders 4 summary cards"
2. "displays correct table count"
3. "displays correct column count"
4. "displays correct database count"
5. "displays correct max depth"
6. "handles summary with zero values"

Test data fixture:
```typescript
const mockSummary: ImpactSummaryData = {
  totalImpacted: 5,
  tableCount: 3,
  columnCount: 5,
  databaseCount: 2,
  byDatabase: { demo_user: 3, analytics: 2 },
  byDepth: { '1': 2, '2': 2, '3': 1 },
};
```

Create lineage-ui/src/api/hooks/useImpact.test.tsx:
Follow the pattern from existing hook tests (useLineage.test.tsx, useAssets.test.tsx).
Test cases:
1. "returns loading state initially"
2. "does not fetch when datasetId is empty"
3. "does not fetch when fieldName is empty"
4. "uses correct query key"

Use the existing test utilities (createTestQueryClient pattern from test files).

After creating tests, run the full test suite:
```bash
cd /Users/Daniel.Tehan/Code/lineage/lineage-ui && npm test -- --run
```

Verify:
- All existing 260+ tests still pass
- All new Impact tests pass
- No test failures or regressions
  </action>
  <verify>
Run `cd /Users/Daniel.Tehan/Code/lineage/lineage-ui && npm test -- --run` and verify all tests pass (0 failures).
Verify new test count is higher than 260 (new tests added).
  </verify>
  <done>ImpactTable.test.tsx has 7 test cases. ImpactSummary.test.tsx has 6 test cases. useImpact.test.tsx has 4 test cases. All 260+ existing tests plus new tests pass with 0 failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification of Phase 1</name>
  <what-built>
Complete Phase 1 implementation:
1. Backend repository layer (extracted from 1455-line monolith)
2. Service layer (LineageService, DatasetService, ImpactService)
3. Flask Blueprints (health + openlineage routes)
4. Impact Analysis API endpoint (GET /api/v2/openlineage/impact/{datasetId}/{fieldName})
5. Impact Analysis frontend (summary cards + TanStack Table)
6. Unit tests for new components
  </what-built>
  <how-to-verify>
1. Start the backend:
   cd lineage-api and python python_server.py
   Expected: Server starts on port 8080 without errors

2. Start the frontend:
   cd lineage-ui and npm run dev
   Expected: Dev server starts on port 3000

3. Open http://localhost:3000 in browser
   Expected: Application loads, asset browser shows databases/tables

4. Navigate to the Impact Analysis page by manually entering a URL like:
   http://localhost:3000/impact/{datasetId}/{fieldName}
   (Use a valid dataset ID and field name from your database)
   NOTE: There is no UI button to navigate to the Impact page in Phase 1. Navigation is via direct URL entry only. A navigation button will be added in a future phase.
   Expected: Impact Analysis page loads showing:
   - Summary cards at top (Tables Affected, Columns Affected, Databases, Max Depth)
   - Sortable table below with Database, Table, Column, Depth, Impact Type columns
   - Depth badges with colors (blue for 1, amber for 2, slate for 3+)
   - Impact type badges (red for direct, amber for indirect)

5. Click column headers in the table
   Expected: Rows sort ascending/descending on clicked column

6. Test existing endpoints still work:
   - http://localhost:8080/api/v2/openlineage/namespaces (should return namespaces)
   - http://localhost:8080/api/v2/openlineage/lineage/database/demo_user (should return lineage graph)
   Expected: Same JSON responses as before refactoring

7. Run frontend tests:
   cd lineage-ui and npm test -- --run
   Expected: All tests pass (260+ existing + new impact tests)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm test -- --run` passes all tests (0 failures)
2. Backend starts without errors
3. Frontend builds and serves correctly
4. Impact Analysis page shows real data from API
5. All existing endpoints maintain backward compatibility
6. No regressions in existing functionality
</verification>

<success_criteria>
Phase 1 complete when ALL are true:
- User can view downstream impact list for any column (IMPACT-01)
- Direct vs indirect dependencies visually distinguished (IMPACT-02)
- Column-level impact counts visible per table (IMPACT-03)
- Summary shows total affected tables/columns/databases (IMPACT-04)
- Backend CTE logic exists in one place (ARCH-01, IMPACT-05)
- Service layer organizes business logic (ARCH-02)
- Flask Blueprints replace direct routes (ARCH-03)
- All existing endpoints backward compatible (ARCH-04)
- maxDepth limits enforced (IMPACT-06)
- TanStack Table used for data display (IMPACT-07)
- Frontend tests pass (260+ existing + new)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-refactoring-impact-analysis-core/01-06-SUMMARY.md`
</output>
