---
phase: 01-foundation-refactoring-impact-analysis-core
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lineage-api/services/__init__.py
  - lineage-api/services/lineage_service.py
  - lineage-api/services/dataset_service.py
  - lineage-api/services/impact_service.py
  - lineage-api/routes/__init__.py
  - lineage-api/routes/health.py
  - lineage-api/routes/openlineage.py
  - lineage-api/python_server.py
autonomous: true

must_haves:
  truths:
    - "All 11 existing API endpoints return identical JSON responses as before refactoring"
    - "Flask Blueprints organize routes by feature area (health, openlineage)"
    - "Service layer orchestrates repository calls and transforms data for API responses"
    - "python_server.py uses Application Factory pattern (create_app function)"
    - "Route handlers contain no SQL - only HTTP concerns (parsing params, returning responses)"
  artifacts:
    - path: "lineage-api/services/lineage_service.py"
      provides: "Lineage graph building logic using LineageRepository"
      exports: ["LineageService"]
    - path: "lineage-api/services/dataset_service.py"
      provides: "Dataset operations using DatasetRepository"
      exports: ["DatasetService"]
    - path: "lineage-api/services/impact_service.py"
      provides: "Impact analysis aggregation using LineageRepository + DatasetRepository"
      exports: ["ImpactService"]
    - path: "lineage-api/routes/openlineage.py"
      provides: "Flask Blueprint for /api/v2/openlineage/* routes"
      exports: ["openlineage_bp"]
    - path: "lineage-api/routes/health.py"
      provides: "Flask Blueprint for /health route"
      exports: ["health_bp"]
    - path: "lineage-api/python_server.py"
      provides: "Application factory using create_app() pattern"
  key_links:
    - from: "lineage-api/routes/openlineage.py"
      to: "lineage-api/services/lineage_service.py"
      via: "service method calls"
      pattern: "lineage_service\\.get_lineage_graph"
    - from: "lineage-api/services/lineage_service.py"
      to: "lineage-api/repositories/lineage_repository.py"
      via: "repository method calls"
      pattern: "self\\.lineage_repo\\.get_downstream_lineage"
    - from: "lineage-api/python_server.py"
      to: "lineage-api/routes/openlineage.py"
      via: "Blueprint registration"
      pattern: "app\\.register_blueprint\\(openlineage_bp\\)"
---

<objective>
Implement service layer and refactor python_server.py into Flask Blueprints

Purpose: Wire the repository layer (from Plan 01) into service classes, then rewire all 11 API endpoints to use services via Flask Blueprints. This is the core refactoring step that eliminates the 1455-line monolith and establishes the layered architecture (Routes -> Services -> Repositories). The impact_service.py created here will be used by Plan 03 for the new impact analysis endpoint.

Output: lineage-api/services/ package with 3 service classes, lineage-api/routes/ package with 2 Blueprints, and python_server.py refactored to Application Factory pattern.
</objective>

<execution_context>
@/Users/Daniel.Tehan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Daniel.Tehan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-refactoring-impact-analysis-core/01-RESEARCH.md
@.planning/phases/01-foundation-refactoring-impact-analysis-core/01-01-SUMMARY.md
@lineage-api/python_server.py
@lineage-api/repositories/lineage_repository.py
@lineage-api/repositories/dataset_repository.py
@lineage-api/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create service layer</name>
  <files>lineage-api/services/__init__.py, lineage-api/services/lineage_service.py, lineage-api/services/dataset_service.py, lineage-api/services/impact_service.py</files>
  <action>
Create lineage-api/services/__init__.py with imports of all three services.

Create lineage-api/services/dataset_service.py with class DatasetService:
- Constructor: __init__(self, dataset_repo: DatasetRepository)
- Methods that delegate to dataset_repo, adding any business logic:
  - list_namespaces() -> dict: Returns {"namespaces": [...]}
  - get_namespace(namespace_id: str) -> dict: Returns namespace dict, raises ValueError if not found
  - list_datasets(namespace_id: str, limit: int, offset: int) -> dict: Returns {"datasets": [...], "pagination": {...}}
  - get_dataset(dataset_id: str) -> dict: Returns dataset with fields, raises ValueError if not found
  - search_datasets(query: str, limit: int) -> dict: Returns {"datasets": [...], "query": query, "count": N}
  - unified_search(query: str, limit: int) -> dict: Returns full search response shape
  - get_dataset_statistics(dataset_id: str) -> dict: Returns stats, raises ValueError if not found
  - get_dataset_ddl(dataset_id: str) -> dict: Returns DDL, raises ValueError if not found
- Service methods should return the EXACT same JSON-serializable dicts that python_server.py currently returns via jsonify(). No changes to response shapes.

Create lineage-api/services/lineage_service.py with class LineageService:
- Constructor: __init__(self, lineage_repo: LineageRepository, dataset_repo: DatasetRepository)
- Methods:
  - get_column_lineage_graph(dataset_id: str, field_name: str, direction: str, max_depth: int) -> dict:
    Replicates the logic from get_openlineage_lineage() (lines 686-925 of python_server.py):
    1. Look up dataset name from dataset_repo
    2. Call lineage_repo.get_upstream_lineage() and/or get_downstream_lineage() based on direction
    3. Build nodes dict and edges list (the node/edge building logic from lines 764-800 and 852-888)
    4. Add root field node if not present
    5. Return {"datasetId": ..., "fieldName": ..., "graph": {"nodes": [...], "edges": [...]}}

  - get_table_lineage_graph(dataset_id: str, direction: str, max_depth: int) -> dict:
    Replicates logic from get_openlineage_table_lineage() (lines 928-1165):
    1. Look up dataset name and namespace
    2. Get all fields for dataset
    3. For each field, call upstream/downstream lineage from repo
    4. Build aggregated nodes and edges
    5. Return {"datasetId": ..., "graph": {"nodes": [...], "edges": [...]}}

  - get_database_lineage_graph(database_name: str, direction: str, max_depth: int) -> dict:
    Replicates logic from get_openlineage_database_lineage() (lines 1172-1443):
    1. Get all datasets in database
    2. Add all field nodes for each dataset
    3. Call lineage_repo.get_database_lineage() with dataset names
    4. Process results, add external nodes with metadata lookups
    5. Return {"databaseName": ..., "direction": ..., "maxDepth": ..., "graph": {"nodes": [...], "edges": [...]}}

  - Helper method _build_node(key, field_name, dataset_name, namespace) -> dict for creating node dicts
  - Helper method _build_edge(source_key, target_key, transformation_type) -> dict for creating edge dicts
  - Helper method _add_lineage_results(records, nodes, edges) to process lineage records into nodes/edges (eliminates duplication between column/table endpoints)

CRITICAL: The node and edge dict shapes MUST exactly match what python_server.py currently produces. Test by comparing JSON outputs.

Create lineage-api/services/impact_service.py with class ImpactService:
- Constructor: __init__(self, lineage_repo: LineageRepository, dataset_repo: DatasetRepository)
- Method: analyze_downstream_impact(dataset_id: str, field_name: str, max_depth: int = 5) -> dict:
  1. Look up dataset name from dataset_repo.get_dataset_name(dataset_id)
  2. Call lineage_repo.get_downstream_lineage(dataset_name, field_name, max_depth)
  3. Build impacted assets list with deduplication (use target_dataset.target_field as key)
  4. For each unique target, parse database_name and table_name from target_dataset (split on ".")
  5. Determine impactType: "direct" if depth == 1, else "indirect"
  6. Calculate summary: totalImpacted, tableCount (unique tables), columnCount, byDatabase (count per db), byDepth (count per depth level), databaseCount (unique databases)
  7. Return:
     ```
     {
       "sourceAsset": {"datasetId": dataset_id, "datasetName": dataset_name, "fieldName": field_name},
       "impactedAssets": [
         {"databaseName": "...", "tableName": "...", "columnName": "...", "depth": N, "impactType": "direct"|"indirect"}
       ],
       "summary": {
         "totalImpacted": N,
         "tableCount": N,
         "columnCount": N,
         "databaseCount": N,
         "byDatabase": {"db_name": count, ...},
         "byDepth": {"1": count, "2": count, ...}
       }
     }
     ```
  - Note: maxDepth default of 5 matches the existing column lineage default. Research suggested 10 but we start conservative.
  </action>
  <verify>
Run `python -c "from services import LineageService, DatasetService, ImpactService; print('services OK')"` from lineage-api/ directory.
Run `python -c "from services.impact_service import ImpactService; print(ImpactService.__init__.__doc__ or 'OK')"` from lineage-api/ directory.
  </verify>
  <done>Three service classes exist: DatasetService (8 methods), LineageService (3 public methods + helpers), ImpactService (1 method). All are importable. Service methods return dict shapes matching current API responses.</done>
</task>

<task type="auto">
  <name>Task 2: Create Flask Blueprints and refactor python_server.py</name>
  <files>lineage-api/routes/__init__.py, lineage-api/routes/health.py, lineage-api/routes/openlineage.py, lineage-api/python_server.py</files>
  <action>
Create lineage-api/routes/__init__.py with exports.

Create lineage-api/routes/health.py with health_bp Blueprint:
- Blueprint('health', __name__)
- Single route: GET /health returning {"status": "ok"}

Create lineage-api/routes/openlineage.py with openlineage_bp Blueprint:
- Blueprint('openlineage', __name__, url_prefix='/api/v2/openlineage')
- Module-level variables for services (lineage_service, dataset_service, impact_service)
- init_services(lineage_svc, dataset_svc, impact_svc) function to set service references
- Route handlers that delegate to services. Each handler:
  1. Parses request parameters (same as current python_server.py)
  2. Calls appropriate service method
  3. Returns jsonify(result)
  4. Catches exceptions and returns {"error": str(e)}, 500 (preserving current error contract)

Routes to create (preserving EXACT URL patterns from python_server.py):
- GET /namespaces -> dataset_service.list_namespaces()
- GET /namespaces/<namespace_id> -> dataset_service.get_namespace(namespace_id)
- GET /namespaces/<namespace_id>/datasets -> dataset_service.list_datasets(namespace_id, limit, offset)
- GET /datasets/<path:dataset_id> -> dataset_service.get_dataset(dataset_id)
- GET /datasets/<path:dataset_id>/statistics -> dataset_service.get_dataset_statistics(dataset_id)
- GET /datasets/<path:dataset_id>/ddl -> dataset_service.get_dataset_ddl(dataset_id)
- GET /datasets/search -> dataset_service.search_datasets(query, limit)
- GET /search -> dataset_service.unified_search(query, limit) [NOTE: This is an alias that hits same URL in current code]
- GET /lineage/<path:dataset_id>/<field_name> -> lineage_service.get_column_lineage_graph(...)
- GET /lineage/table/<path:dataset_id> -> lineage_service.get_table_lineage_graph(...)
- GET /lineage/database/<database_name> -> lineage_service.get_database_lineage_graph(...)

IMPORTANT: The search route uses /datasets/search and the unified search uses /search. In the CURRENT python_server.py, the /search route actually calls the same datasets/search backend logic (line 606-680 is unified_search). Make sure to preserve this behavior.

For ValueError exceptions from services (dataset not found), catch and return 404 with {"error": message}. For all other exceptions, return 500 with {"error": str(e)} or {"error": "Internal server error"} matching current behavior.

Refactor lineage-api/python_server.py to Application Factory:
- Remove all inline route definitions (the 11 @app.route handlers)
- Remove inline DB_CONFIG and get_db_connection (now in config.py)
- Remove validate_required_credentials (now in config.py)
- Keep CORS configuration
- Create create_app() function that:
  1. Creates Flask app
  2. Configures CORS (same origins as current: localhost:3000, 3001, 3004, 5173)
  3. Creates DB connection via config.get_db_connection()
  4. Instantiates repositories (LineageRepository, DatasetRepository)
  5. Instantiates services (LineageService, DatasetService, ImpactService)
  6. Calls init_services on openlineage_bp
  7. Registers health_bp and openlineage_bp
  8. Returns app
- Keep the if __name__ == "__main__" block but have it call create_app()

The connection lifecycle: For now, create a single connection at app startup and share it across repositories (matching current behavior where get_db_connection() is called per request). The per-request pattern will be preserved by having services create new repository instances per request using Flask's before_request hook, OR by having repositories use get_db_connection() internally. Choose the simpler approach: have repositories call get_db_connection() at method invocation time rather than storing a persistent connection. This way BaseRepository.__init__() takes no args, and each repository method calls get_db_connection() when it needs a cursor. This matches the current per-request connection pattern in python_server.py.

CRITICAL BACKWARD COMPATIBILITY: After refactoring, every single endpoint must produce IDENTICAL JSON output for the same inputs. The error response format must also be preserved ({"error": string} for 404/500).
  </action>
  <verify>
Run `cd /Users/Daniel.Tehan/Code/lineage/lineage-api && python -c "from python_server import create_app; app = create_app(); print('App created, routes:'); print([rule.rule for rule in app.url_map.iter_rules()])"` to verify all routes registered.
Verify all 11 API endpoint URLs are present in the route map.
Run `cd /Users/Daniel.Tehan/Code/lineage/lineage-ui && npm run build` to verify frontend still builds (no API contract changes).
  </verify>
  <done>python_server.py uses create_app() factory pattern. All 11 API endpoints registered via Blueprints. Route handlers contain no SQL, only HTTP concerns. Service layer mediates all database access. Frontend build succeeds (no API changes).</done>
</task>

</tasks>

<verification>
1. `python -c "from python_server import create_app; app = create_app()"` succeeds
2. All 11 API endpoint URLs appear in app.url_map
3. Route handlers in routes/openlineage.py contain zero SQL statements
4. python_server.py is under 60 lines (factory + main block only)
5. Service methods return dicts matching current API response shapes
6. `cd lineage-ui && npm run build` succeeds
</verification>

<success_criteria>
- python_server.py refactored from 1455 lines to under 60 lines using create_app() factory
- All 11 existing endpoints accessible at same URLs with same response formats
- Service layer handles business logic (graph building, aggregation)
- Route handlers only handle HTTP concerns (param parsing, response formatting)
- ImpactService exists and is ready for the new endpoint (Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-refactoring-impact-analysis-core/01-02-SUMMARY.md`
</output>
