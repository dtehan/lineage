---
phase: 19-layout-engine-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lineage-ui/src/components/domain/LineageGraph/DatabaseLineageGraph.tsx
autonomous: true

must_haves:
  truths:
    - "DatabaseLineageGraph runs layout in a Web Worker, not on the main thread"
    - "Switching direction rapidly does not produce stale or doubled layouts"
    - "Layout progress indicators still show during Worker computation"
  artifacts:
    - path: "lineage-ui/src/components/domain/LineageGraph/DatabaseLineageGraph.tsx"
      provides: "Worker-based layout with generation-counter race protection"
      contains: "useLayoutWorker"
  key_links:
    - from: "lineage-ui/src/components/domain/LineageGraph/DatabaseLineageGraph.tsx"
      to: "lineage-ui/src/components/domain/LineageGraph/hooks/useLayoutWorker.ts"
      via: "useLayoutWorker() hook import"
      pattern: "useLayoutWorker"
---

<objective>
Migrate DatabaseLineageGraph layout from main-thread `layoutGraph()` to the existing Web Worker via `useLayoutWorker`, and replace the broken `cancelled` boolean race-condition guard with a generation counter.

Purpose: Database lineage graphs with 200-500 tables currently block the main thread during layout computation, causing visible frame drops. Additionally, rapid direction switching can race two layout Promises against each other, producing stale or doubled layouts.

Output: DatabaseLineageGraph.tsx using Worker-based layout with generation-counter cancellation.
</objective>

<execution_context>
@/Users/Daniel.Tehan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Daniel.Tehan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-layout-engine-foundation/19-RESEARCH.md

@lineage-ui/src/components/domain/LineageGraph/DatabaseLineageGraph.tsx
@lineage-ui/src/components/domain/LineageGraph/hooks/useLayoutWorker.ts
@lineage-ui/src/workers/layout.types.ts
@lineage-ui/src/workers/layout.worker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire useLayoutWorker and generation counter into DatabaseLineageGraph</name>
  <files>lineage-ui/src/components/domain/LineageGraph/DatabaseLineageGraph.tsx</files>
  <action>
Make the following targeted edits to `DatabaseLineageGraph.tsx`:

**1. Add `useLayoutWorker` import (near existing hook imports):**
Add `import { useLayoutWorker } from './hooks/useLayoutWorker';` alongside the other hook imports.

**2. Add `useLayoutWorker` hook call and generation ref inside the component:**
Inside `DatabaseLineageGraphInner`, near the top where other hooks/refs are declared:
- Add `const { layoutGraph: workerLayoutGraph } = useLayoutWorker();`
- Add `const generationRef = useRef(0);`

**3. Replace the layout useEffect (lines 160-198) with Worker + generation counter:**
The existing useEffect starting at `// Update nodes/edges when data changes` needs these changes:

a. Remove the `let cancelled = false;` line.

b. Replace the `layoutGraph(converted.nodes, converted.edges, { onProgress: (p) => setProgress(p) })` call with:
```typescript
// Increment generation counter — any older in-flight layout is now stale
const generation = ++generationRef.current;

setProgress(35); // Entering layout stage

// Strip onProgress (not structured-clone-able across Worker boundary)
// and run layout in Worker thread to keep main thread responsive
workerLayoutGraph(converted.nodes, converted.edges, {
  direction,
})
```

c. In the `.then()` handler, replace `if (cancelled) return;` with:
```typescript
if (generation !== generationRef.current) return; // Stale result — discard
```

d. In the `.catch()` handler, replace `if (cancelled) return;` with:
```typescript
if (generation !== generationRef.current) return; // Stale result — discard
```

e. In the cleanup function, remove `cancelled = true;` and keep only `reset();`. The generation counter handles staleness — no boolean needed.

f. Add `direction` and `workerLayoutGraph` to the dependency array of this useEffect (they are referenced inside the effect body).

**4. Remove unused direct `layoutGraph` import:**
Change the import `import { layoutGraph, type TableNodeData } from '../../../utils/graph/layoutEngine';` to just `import { type TableNodeData } from '../../../utils/graph/layoutEngine';` since `layoutGraph` is no longer called directly.

**Critical constraint:** Do NOT pass `onProgress` to `workerLayoutGraph`. Functions cannot be structured-cloned across the Worker boundary (Comlink uses structured clone). Instead, emit fixed progress milestones manually:
- `setProgress(35)` before the Worker call (already done above)
- `setProgress(90)` at the start of the `.then()` handler (after the staleness check)
- The existing `setStage('rendering')` and `setStage('complete')` handle the rest

**What NOT to change:** Everything outside this useEffect and the imports stays untouched. The rest of the component (filtering, viewport, click handlers, JSX) is not affected.
  </action>
  <verify>
Run `cd /Users/Daniel.Tehan/Code/lineage/lineage-ui && npx vitest run --reporter=verbose 2>&1 | tail -20` to confirm no test regressions.

Manually verify in the modified file:
1. `layoutGraph` is no longer imported from `layoutEngine`
2. `useLayoutWorker` is imported and called
3. `generationRef` is declared as `useRef(0)`
4. The generation counter pattern replaces the `cancelled` boolean
5. No `onProgress` callback is passed to `workerLayoutGraph`
6. `direction` is in the dependency array of the layout useEffect
  </verify>
  <done>
DatabaseLineageGraph runs layout via Worker (off main thread) and uses generation counter to discard stale results from rapid direction changes. No `onProgress` callback crosses the Worker boundary. All existing tests pass.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "layoutGraph" lineage-ui/src/components/domain/LineageGraph/DatabaseLineageGraph.tsx` shows NO direct `layoutGraph()` call — only the type import and `workerLayoutGraph` usage
2. `grep -n "useLayoutWorker" lineage-ui/src/components/domain/LineageGraph/DatabaseLineageGraph.tsx` shows the import and hook call
3. `grep -n "generationRef" lineage-ui/src/components/domain/LineageGraph/DatabaseLineageGraph.tsx` shows the ref declaration and two staleness checks (`.then()` and `.catch()`)
4. `grep -n "cancelled" lineage-ui/src/components/domain/LineageGraph/DatabaseLineageGraph.tsx` returns NO matches — the boolean is fully replaced
5. `grep -n "onProgress" lineage-ui/src/components/domain/LineageGraph/DatabaseLineageGraph.tsx` returns NO matches — the callback is not passed to Worker
6. All 260+ frontend tests pass: `cd lineage-ui && npx vitest run`
</verification>

<success_criteria>
- DatabaseLineageGraph.tsx imports and calls `useLayoutWorker()` instead of direct `layoutGraph()`
- Generation counter (`useRef(0)`) replaces `let cancelled = false` pattern
- No `onProgress` callback crosses the Worker boundary
- Fixed progress milestones (35 before, 90 after) maintain loading UX
- All existing Vitest tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/19-layout-engine-foundation/19-01-SUMMARY.md`
</output>
