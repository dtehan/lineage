---
phase: 23-standalone-table-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lineage-api/services/lineage_service.py
  - lineage-ui/src/components/domain/LineageGraph/LineageGraph.tsx
  - lineage-ui/src/components/domain/LineageGraph/LineageGraph.test.tsx
autonomous: true

must_haves:
  truths:
    - "Backend returns valid {nodes, edges} response (not 404) for tables with no lineage edges"
    - "Backend returns valid {nodes:[], edges:[]} response (not 404) for tables with no OL_DATASET_FIELD entries"
    - "User sees a single table node card with columns rendered in the graph view for a table with no lineage"
    - "User sees a 'No lineage connections' informational banner (blue, not red error) when viewing a table with zero lineage edges"
    - "The informational banner is rendered alongside the ReactFlow canvas (not replacing it)"
  artifacts:
    - path: "lineage-api/services/lineage_service.py"
      provides: "get_table_lineage_graph returns valid empty graph instead of raising DatasetNotFoundError for no-fields case"
      contains: "nodes.*edges"
    - path: "lineage-ui/src/components/domain/LineageGraph/LineageGraph.tsx"
      provides: "Inline informational banner for hasNoLineageData state; ReactFlow canvas still renders"
      contains: "No lineage connections"
  key_links:
    - from: "lineage-api/services/lineage_service.py"
      to: "frontend hasNoLineageData branch"
      via: "API response with edges: [] triggers banner display"
      pattern: "edges.*length.*0"
---

<objective>
Fix standalone table rendering so tables with no lineage relationships display as a valid single-node graph with columns and an informational banner -- not as an error state or a blank screen that replaces the canvas.

Purpose: After Phase 22 populated the full system catalog, users can now navigate to any table in the Asset Browser. Tables without lineage data currently trigger a `DatasetNotFoundError` on the backend (returning 404) or show a full-screen empty state that replaces the ReactFlow canvas entirely. Both behaviors are incorrect for the v6.0 goal of making every table browsable and renderable.

Output: Backend returns valid `{nodes, edges}` for all tables; frontend renders node card with columns and shows "No lineage connections" banner inline above the canvas.
</objective>

<execution_context>
@/Users/Daniel.Tehan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Daniel.Tehan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-standalone-table-rendering/23-RESEARCH.md
@lineage-api/services/lineage_service.py
@lineage-ui/src/components/domain/LineageGraph/LineageGraph.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — Return valid graph response for tables with no fields or no lineage</name>
  <files>lineage-api/services/lineage_service.py</files>
  <action>
In `lineage_service.py`, find `get_table_lineage_graph()` method (around line 170-172). The current code:

```python
fields = self.dataset_repo.get_dataset_fields(dataset_id)
if not fields:
    raise DatasetNotFoundError(f"No fields found for dataset: {dataset_id}")
```

Replace the `raise DatasetNotFoundError(...)` with an early return of a valid empty graph:

```python
fields = self.dataset_repo.get_dataset_fields(dataset_id)
if not fields:
    # Dataset exists in catalog but has no fields populated — valid state, not an error.
    # Return a valid empty graph so the frontend can render the informational banner.
    return {
        "datasetId": dataset_id,
        "graph": {
            "nodes": [],
            "edges": []
        }
    }
```

This preserves the existing behavior of `get_dataset_with_namespace()` (which already correctly raises `DatasetNotFoundError` if the dataset itself does not exist in `OL_DATASET`) while fixing the no-fields case to return a valid response.

Do NOT change any other code in `get_table_lineage_graph()`. The rest of the method handles the normal case where fields exist and lineage may or may not be present (fields with no lineage edges already work correctly — they return nodes with `edges: []`).

Also do NOT remove or modify the ELK early-exit gate in the frontend (`LineageGraph.tsx` lines 281-288) — it correctly handles zero-edge graphs and must remain intact.
  </action>
  <verify>
Run `cd /Users/Daniel.Tehan/Code/lineage/lineage-api && python -c "from services.lineage_service import LineageService; print('Import OK')"` to verify no syntax errors.

Confirm the change by reading `lineage_service.py` and verifying:
1. The `DatasetNotFoundError` raise for "No fields found" is removed
2. An early return with `{"datasetId": dataset_id, "graph": {"nodes": [], "edges": []}}` exists
3. The existing `DatasetNotFoundError` raise for the dataset-not-in-OL_DATASET case (from `get_dataset_with_namespace`) is untouched
  </verify>
  <done>Backend returns valid `{nodes:[], edges:[]}` JSON (not 404/error) when a table exists in OL_DATASET but has no entries in OL_DATASET_FIELD; backend continues to return nodes with `edges:[]` when fields exist but have no lineage edges (already working)</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — Replace full-screen empty state with inline informational banner</name>
  <files>lineage-ui/src/components/domain/LineageGraph/LineageGraph.tsx, lineage-ui/src/components/domain/LineageGraph/LineageGraph.test.tsx</files>
  <action>
In `LineageGraph.tsx`, make two changes:

**Change 1: Remove the full-screen empty state replacement block (lines 678-706)**

Delete or comment out the entire `if (hasNoLineageData) { return (...) }` block that currently replaces the ReactFlow canvas with a full-screen centered empty state. Keep the `hasNoLineageData` variable declaration — it will be used for the inline banner.

The `hasNoLineageData` variable should remain:
```tsx
const hasNoLineageData = data && data.graph && data.graph.edges?.length === 0;
```

But remove the `if (hasNoLineageData) { return (...); }` block that follows it. This block currently short-circuits the entire render, preventing the ReactFlow canvas (and the table node) from rendering.

**Change 2: Add an inline informational banner inside the render return**

After the `ProgressBanner` component (around line 764) and before the `{/* Graph View */}` comment (around line 776), add a "No lineage connections" informational banner that renders only when `hasNoLineageData` is true:

```tsx
{/* No lineage connections informational banner — shown inline alongside canvas, not replacing it */}
{hasNoLineageData && (
  <div
    role="status"
    aria-live="polite"
    data-testid="no-lineage-banner"
    className="flex items-center gap-2 px-4 py-2 bg-blue-50 border-b border-blue-100 text-blue-700 text-sm"
  >
    <Info size={16} className="shrink-0" />
    <span>No lineage connections found for this table. Showing columns only.</span>
  </div>
)}
```

Make sure `Info` is imported from `lucide-react`. Check the existing imports at the top of the file — if `Info` is not already imported, add it to the existing lucide-react import statement.

The banner uses `role="status"` + `aria-live="polite"` for accessibility (informational, not an error). The blue color scheme (`bg-blue-50`, `text-blue-700`) distinguishes it from the red error state (`text-red-500`, `role="alert"`). The `data-testid="no-lineage-banner"` enables test assertions.

**Change 3: Update tests in LineageGraph.test.tsx**

Add or update test cases to verify:
1. When API returns `edges: []` with nodes, the `no-lineage-banner` is rendered (not an error state)
2. When API returns `edges: []`, the ReactFlow canvas is still in the DOM (not replaced by full-screen empty state)
3. The banner text contains "No lineage connections"

Use the existing test patterns in the file. Mock the API response to return `{ datasetId: "test.table", graph: { nodes: [...], edges: [] } }` with at least one node to simulate the no-lineage-with-fields case.
  </action>
  <verify>
Run `cd /Users/Daniel.Tehan/Code/lineage/lineage-ui && npx vitest run src/components/domain/LineageGraph/LineageGraph.test.tsx --reporter=verbose` to verify tests pass.

Manually verify by reading `LineageGraph.tsx`:
1. The old `if (hasNoLineageData) { return (...); }` block (full-screen replacement) is removed
2. The new inline banner with `data-testid="no-lineage-banner"` is present inside the main return, between ProgressBanner and the graph view
3. `Info` is imported from `lucide-react`
4. The `hasNoLineageData` variable is still declared and used for the banner condition
  </verify>
  <done>ReactFlow canvas renders for tables with zero lineage edges (single node card visible with columns); informational "No lineage connections" banner appears inline above the canvas in blue (not red error); existing functionality for tables with lineage edges is unchanged; tests pass</done>
</task>

</tasks>

<verification>
1. Backend: `get_table_lineage_graph()` returns `{nodes:[], edges:[]}` for no-fields datasets (not 404)
2. Backend: `get_table_lineage_graph()` returns `{nodes:[...], edges:[]}` for datasets with fields but no lineage (already works, unchanged)
3. Frontend: `hasNoLineageData` renders inline blue banner, NOT full-screen replacement
4. Frontend: ReactFlow canvas renders below banner with table node card visible
5. Frontend: ELK early-exit gate at line 281 remains intact (prevents hang)
6. Tests: `LineageGraph.test.tsx` tests pass including new no-lineage-banner assertions
</verification>

<success_criteria>
- User navigates to any table with no lineage data and sees a single node card with its columns rendered in the graph view
- User sees "No lineage connections" informational banner (blue, not red error) when viewing a table with zero lineage edges
- Backend returns valid `{nodes, edges}` response for tables with no lineage — never a 404 or error response
- All existing LineageGraph tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/23-standalone-table-rendering/23-01-SUMMARY.md`
</output>
