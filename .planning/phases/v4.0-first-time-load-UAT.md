---
status: complete
phase: v4.0-first-time-load (phases 14-18)
source: 14-01-SUMMARY.md, 14-02-SUMMARY.md, 14-03-SUMMARY.md, 15-01-SUMMARY.md, 16-01-SUMMARY.md, 16-02-SUMMARY.md, 17-01-SUMMARY.md, 17-02-SUMMARY.md, 18-01-SUMMARY.md
started: 2026-02-21T20:00:00Z
updated: 2026-02-21T20:10:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Graph Status Endpoint
expected: GET /api/v2/graph/status returns JSON with ready, node_count, edge_count, last_rebuild_iso, memory_bytes. When warm, ready=true and counts are non-zero.
result: pass

### 2. Column Lineage Graph Loads
expected: Select a column in the Asset Browser and click to view lineage. The lineage graph renders with table nodes and edge connections showing data flow. Results should match what you've seen before (BFS path serving same data as old CTE path).
result: pass

### 3. Fast Depth-1 Loading
expected: Click a column to view lineage. The full-screen spinner dismisses quickly as depth-1 results appear first (~200ms). You don't wait for the entire graph to load before seeing something.
result: pass

### 4. Background Full-Depth Expansion
expected: After depth-1 graph appears, the full-depth graph expands automatically in the background — no second click or manual action required. Additional nodes/edges appear as deeper levels load.
result: pass

### 5. Progress Banner During Background Fetch
expected: While full-depth data loads in the background (after depth-1 is visible), a thin inline banner appears between the toolbar and graph saying "Expanding to full depth..." with a spinner icon.
result: pass

### 6. No Layout Jitter on Depth Expansion
expected: Nodes visible in the depth-1 graph do NOT visibly jump or reposition when the full-depth data arrives and deeper nodes are added. The layout remains stable.
result: pass

### 7. Per-Stage Timing in Progress Banner
expected: During the background full-depth fetch, the progress banner shows per-stage timing like "Fetch: XXms | Layout: XXms | Expanding to full depth..."
result: pass

### 8. Post-Render Timing Summary
expected: After the graph fully loads (complete), a timing summary appears showing "Loaded in: Fetch Xms / Layout Xms / Render Xms" (or similar format showing per-stage durations).
result: pass

### 9. Server-Timing Response Headers
expected: Lineage API responses include a Server-Timing header with timing metrics (bfs_upstream, bfs_downstream, total). Verified programmatically via curl.
result: pass

### 10. Cache Invalidation Triggers Graph Rebuild
expected: POST /api/v2/cache/invalidate with JSON body {"all": true} returns response including "graph_rebuild_triggered": true.
result: pass

### 11. Table Lineage Still Works
expected: Navigate to table-level lineage view. The table lineage graph renders correctly showing relationships between tables.
result: pass

## Summary

total: 11
passed: 11
issues: 0
pending: 0
skipped: 0

## Gaps

[none]
