---
phase: 05-frontend-rendering-optimization
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - lineage-ui/src/components/domain/LineageGraph/hooks/useProfiler.ts
  - lineage-ui/src/components/domain/LineageGraph/hooks/index.ts
  - lineage-ui/src/components/domain/LineageGraph/LineageGraph.tsx
  - lineage-ui/src/utils/graph/disableTransitions.ts
  - lineage-ui/src/index.css
autonomous: true

must_haves:
  truths:
    - "React Profiler wraps LineageGraph component tree and logs re-render count + duration in development"
    - "CSS transitions and animations are disabled when graph has >200 nodes"
    - "CSS transitions are re-enabled when graph drops below threshold or component unmounts"
    - "prefers-reduced-motion media query is respected (transitions already disabled for that preference)"
    - "All existing 260+ frontend unit tests and 21 E2E tests pass"
  artifacts:
    - path: "lineage-ui/src/components/domain/LineageGraph/hooks/useProfiler.ts"
      provides: "React Profiler hook collecting render metrics"
      contains: "useProfiler"
    - path: "lineage-ui/src/utils/graph/disableTransitions.ts"
      provides: "CSS transition toggle utility for large graphs"
      contains: "toggleTransitions"
    - path: "lineage-ui/src/index.css"
      provides: "no-transitions CSS class for disabling animations"
      contains: "no-transitions"
  key_links:
    - from: "lineage-ui/src/components/domain/LineageGraph/LineageGraph.tsx"
      to: "lineage-ui/src/components/domain/LineageGraph/hooks/useProfiler.ts"
      via: "useProfiler hook import + Profiler wrapper"
      pattern: "Profiler.*onRender"
    - from: "lineage-ui/src/components/domain/LineageGraph/LineageGraph.tsx"
      to: "lineage-ui/src/utils/graph/disableTransitions.ts"
      via: "useEffect calling toggleTransitions based on node count"
      pattern: "toggleTransitions"
---

<objective>
Add React Profiler instrumentation to measure component re-render frequency, and disable CSS transitions for large graphs (>200 nodes) to eliminate animation jank.

Purpose: React Profiler provides measurable data on re-render frequency (FRONTEND-02 requirement) and establishes a baseline for optimization validation. Disabling CSS transitions for large graphs (FRONTEND-05) prevents animation jank when React Flow renders 200+ nodes simultaneously.

Output: useProfiler hook, Profiler wrapper on LineageGraph, CSS transition disable utility with >200 node threshold, updated index.css with no-transitions class.
</objective>

<execution_context>
@/Users/Daniel.Tehan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Daniel.Tehan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-frontend-rendering-optimization/05-RESEARCH.md
@.planning/phases/05-frontend-rendering-optimization/05-01-SUMMARY.md

Key source files to read before implementing:
@lineage-ui/src/components/domain/LineageGraph/LineageGraph.tsx
@lineage-ui/src/components/domain/LineageGraph/hooks/index.ts
@lineage-ui/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create React Profiler hook and CSS transition disable utility</name>
  <files>
    lineage-ui/src/components/domain/LineageGraph/hooks/useProfiler.ts
    lineage-ui/src/utils/graph/disableTransitions.ts
    lineage-ui/src/index.css
    lineage-ui/src/components/domain/LineageGraph/hooks/index.ts
  </files>
  <action>
1. Create `lineage-ui/src/components/domain/LineageGraph/hooks/useProfiler.ts`:
   - Define `ProfilerMetrics` interface: `{ id: string; phase: 'mount' | 'update'; actualDuration: number; baseDuration: number; startTime: number; commitTime: number }`
   - Export `useProfiler(id: string)` hook that:
     - Uses `useRef<ProfilerMetrics[]>([])` to accumulate metrics
     - Returns `onRender` callback (matching React Profiler's `onRender` signature) that pushes metrics to the ref
     - In development mode (`import.meta.env.DEV`), logs update-phase renders to console: `[Profiler] ${id} re-render: ${actualDuration.toFixed(2)}ms (base: ${baseDuration.toFixed(2)}ms)`
     - Returns `getMetrics()` function that returns `metrics.current`
     - Returns `clearMetrics()` function that resets `metrics.current = []`
     - Returns `getRenderCount()` function that returns `metrics.current.filter(m => m.phase === 'update').length`
   - All callbacks wrapped with `useCallback` for stability

2. Create `lineage-ui/src/utils/graph/disableTransitions.ts`:
   - Export constant `TRANSITION_THRESHOLD = 200`
   - Export `toggleTransitions(enable: boolean): void` function that:
     - Gets `document.documentElement`
     - If `enable` is true: removes `no-transitions` class from root
     - If `enable` is false: adds `no-transitions` class to root
   - Export `shouldDisableTransitions(nodeCount: number): boolean` that returns `nodeCount > TRANSITION_THRESHOLD`

3. Update `lineage-ui/src/index.css`:
   - Add the `no-transitions` CSS class BEFORE the existing `@media (prefers-reduced-motion)` block:
     ```css
     /* Performance: Disable transitions for large graphs (>200 nodes) */
     .no-transitions *,
     .no-transitions *::before,
     .no-transitions *::after {
       transition-property: none !important;
       animation: none !important;
     }
     ```
   - Note: Do NOT add `transform: none !important` (from research) — that would break React Flow's CSS transforms for node positioning. Only disable transitions and animations.
   - The existing `@media (prefers-reduced-motion: reduce)` block already handles accessibility. The new `.no-transitions` class is for performance-triggered disabling.

4. Update `lineage-ui/src/components/domain/LineageGraph/hooks/index.ts`:
   - Add `export { useProfiler } from './useProfiler';`
  </action>
  <verify>
- `cd lineage-ui && npx tsc --noEmit` passes
- Read `useProfiler.ts` and confirm it implements the React Profiler `onRender` callback signature
- Read `disableTransitions.ts` and confirm TRANSITION_THRESHOLD is 200
- Read `index.css` and confirm `.no-transitions` class exists WITHOUT `transform: none`
- Read `hooks/index.ts` and confirm `useProfiler` is exported
  </verify>
  <done>
useProfiler hook created with metrics collection, render counting, and dev-mode console logging. disableTransitions utility created with 200-node threshold. CSS no-transitions class added to index.css (without transform: none to preserve React Flow positioning).
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Profiler and transition disabling into LineageGraph</name>
  <files>
    lineage-ui/src/components/domain/LineageGraph/LineageGraph.tsx
  </files>
  <action>
1. Add imports to LineageGraph.tsx:
   - `import { Profiler } from 'react';` (add to existing React imports)
   - `import { useProfiler } from './hooks';` (add alongside existing hook imports)
   - `import { toggleTransitions, shouldDisableTransitions } from '../../../utils/graph/disableTransitions';`

2. In `LineageGraphInner` component, add Profiler hook:
   - `const { onRender } = useProfiler('LineageGraph');`

3. Add CSS transition management `useEffect`:
   - Add a new `useEffect` that depends on `filteredNodesAndEdges.filteredNodes.length`:
     ```typescript
     // Disable CSS transitions for large graphs to prevent animation jank (FRONTEND-05)
     useEffect(() => {
       const nodeCount = filteredNodesAndEdges.filteredNodes.length;
       if (shouldDisableTransitions(nodeCount)) {
         toggleTransitions(false);
       } else {
         toggleTransitions(true);
       }
       // Re-enable transitions on unmount
       return () => toggleTransitions(true);
     }, [filteredNodesAndEdges.filteredNodes.length]);
     ```
   - Place this effect after the existing `useDatabaseClustersFromNodes` call (around line 170)

4. Wrap the main return JSX with React Profiler:
   - The outer `LineageGraph` component (the one with `ReactFlowProvider`) wraps `LineageGraphInner`
   - Wrap the return of `LineageGraphInner` (the `<div ref={wrapperRef} ...>` element) with `<Profiler id="LineageGraph" onRender={onRender}>...</Profiler>`
   - This captures all re-renders of the entire graph component tree
   - Note: The early returns (showProgress loading state, error state, empty state) should NOT be wrapped in the Profiler — only the main graph render

5. Do NOT wrap the early return paths (loading, error, empty) with Profiler. Only the main interactive graph view needs profiling — the loading/error states are trivial renders.

6. Existing memoization audit: Review and confirm the following are already correctly memoized (document findings, do NOT change unless broken):
   - `nodeTypes` and `edgeTypes` are already module-level constants (line 54-60) — CORRECT, stable references
   - `onNodeClick`, `onEdgeClick`, `onPaneClick` are already wrapped in `useCallback` — CORRECT
   - `filteredNodesAndEdges` is already wrapped in `useMemo` — CORRECT
   - `clusters` uses `useDatabaseClustersFromNodes` which uses `useMemo` internally — CORRECT
   - This satisfies FRONTEND-03 (memoization audit complete)
  </action>
  <verify>
- `cd lineage-ui && npx tsc --noEmit` passes
- `cd lineage-ui && npm test -- --run` — all 260+ tests pass
- Read LineageGraph.tsx and confirm:
  - `<Profiler id="LineageGraph" onRender={onRender}>` wraps the main graph JSX
  - `useEffect` with `toggleTransitions` exists and depends on node count
  - Cleanup function calls `toggleTransitions(true)`
  - Early returns (loading, error, empty) are NOT wrapped in Profiler
  </verify>
  <done>
React Profiler wraps LineageGraph component tree, logging re-render frequency in dev mode. CSS transitions automatically disabled for graphs >200 nodes. Transitions re-enabled on unmount or when node count drops below threshold. Memoization audit confirms existing patterns are correct (nodeTypes/edgeTypes module-level, callbacks useCallback-wrapped, filteredNodesAndEdges useMemo-wrapped). All tests pass.
  </done>
</task>

</tasks>

<verification>
1. `cd lineage-ui && npm test -- --run` — all 260+ frontend unit tests pass
2. `cd lineage-ui && npx tsc --noEmit` — no TypeScript errors
3. `cd lineage-ui && npm run build` — production build succeeds
4. LineageGraph.tsx contains `<Profiler>` wrapper around main graph render
5. LineageGraph.tsx contains `useEffect` with `toggleTransitions` based on node count threshold
6. `index.css` contains `.no-transitions` class without `transform: none`
7. Memoization audit documented: nodeTypes/edgeTypes stable, callbacks memoized, filteredNodesAndEdges memoized
</verification>

<success_criteria>
- React Profiler instrumentation active on LineageGraph (FRONTEND-02)
- Memoization audit complete — existing patterns confirmed correct (FRONTEND-03)
- CSS transitions disabled for >200 node graphs (FRONTEND-05)
- prefers-reduced-motion already respected via existing CSS (accessibility)
- All 260+ frontend unit tests pass
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-rendering-optimization/05-02-SUMMARY.md`
</output>
