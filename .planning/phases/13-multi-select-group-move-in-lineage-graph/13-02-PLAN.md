---
phase: 13-multi-select-group-move-in-lineage-graph
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - lineage-ui/src/components/domain/LineageGraph/hooks/useMultiSelect.test.ts
  - lineage-ui/src/components/domain/LineageGraph/Toolbar.test.tsx
autonomous: false

must_haves:
  truths:
    - "useMultiSelect hook returns isMultiSelectMode, onSelectionChange, and onSelectionDragStart"
    - "onSelectionDragStart sets hasUserInteractedRef to true"
    - "onSelectionChange clears column selection when nodes are multi-selected"
    - "Toolbar renders multi-select toggle button with correct aria attributes"
    - "Toolbar toggle button shows active state when isMultiSelectMode is true"
    - "User verifies Cmd+click multi-select, group drag, toolbar toggle, and Escape exit work in browser"
  artifacts:
    - path: "lineage-ui/src/components/domain/LineageGraph/hooks/useMultiSelect.test.ts"
      provides: "Unit tests for useMultiSelect hook"
      min_lines: 40
    - path: "lineage-ui/src/components/domain/LineageGraph/Toolbar.test.tsx"
      provides: "Updated tests covering multi-select toggle button"
      contains: "multi-select"
  key_links:
    - from: "lineage-ui/src/components/domain/LineageGraph/hooks/useMultiSelect.test.ts"
      to: "lineage-ui/src/components/domain/LineageGraph/hooks/useMultiSelect.ts"
      via: "Tests import and exercise useMultiSelect hook"
      pattern: "useMultiSelect"
---

<objective>
Add unit tests for the multi-select feature and verify the complete interactive experience in the browser.

Purpose: Ensure multi-select behavior is correct and regression-safe, and confirm the user experience works end-to-end.
Output: Test files for useMultiSelect hook and updated Toolbar tests, plus human verification of the interactive feature.
</objective>

<execution_context>
@/Users/Daniel.Tehan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Daniel.Tehan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-multi-select-group-move-in-lineage-graph/13-01-SUMMARY.md
@lineage-ui/src/components/domain/LineageGraph/hooks/useMultiSelect.ts
@lineage-ui/src/components/domain/LineageGraph/Toolbar.tsx
@lineage-ui/src/components/domain/LineageGraph/Toolbar.test.tsx
@lineage-ui/src/components/domain/LineageGraph/hooks/useSmartViewport.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for useMultiSelect hook and update Toolbar tests</name>
  <files>
    lineage-ui/src/components/domain/LineageGraph/hooks/useMultiSelect.test.ts
    lineage-ui/src/components/domain/LineageGraph/Toolbar.test.tsx
  </files>
  <action>
**useMultiSelect.test.ts** — Create unit tests for the useMultiSelect hook. Follow existing hook test patterns (see useSmartViewport.test.ts, useFitToSelection.test.ts for reference patterns).

Test cases:
1. `returns isMultiSelectMode from store` — Mock useLineageStore to return isMultiSelectMode: false, verify hook returns it
2. `returns isMultiSelectMode true when store has it true` — Same but with true
3. `onSelectionDragStart sets hasUserInteractedRef to true` — Create a ref, pass to hook, call onSelectionDragStart, verify ref.current === true
4. `onSelectionChange clears highlight when nodes selected` — Mock useLineageStore.getState() to have a selectedAssetId, call onSelectionChange with 1+ nodes, verify clearHighlight and closePanel were called
5. `onSelectionChange does nothing when no nodes selected` — Call with empty nodes array, verify clearHighlight NOT called
6. `onSelectionChange does nothing when no prior selection` — Mock getState with selectedAssetId: null, call with nodes, verify clearHighlight NOT called

Mock setup:
- Mock `../../../../stores/useLineageStore` module
- Use `vi.fn()` for store methods (clearHighlight, closePanel)
- Use `renderHook` from `@testing-library/react` for hook testing

**Toolbar.test.tsx** — Add new tests to the existing Toolbar test suite:

Find the existing test file and add a new `describe('multi-select toggle')` block with these tests:
1. `renders multi-select toggle button when onToggleMultiSelectMode provided` — Render Toolbar with `onToggleMultiSelectMode` prop, verify button with data-testid="multi-select-toggle" exists
2. `does not render multi-select toggle when onToggleMultiSelectMode not provided` — Render without prop, verify button absent
3. `calls onToggleMultiSelectMode when clicked` — Render with prop, click button, verify callback called once
4. `shows active styling when isMultiSelectMode is true` — Render with `isMultiSelectMode={true}`, verify button has `aria-pressed="true"` and `bg-blue-100` class
5. `shows inactive styling when isMultiSelectMode is false` — Render with `isMultiSelectMode={false}`, verify `aria-pressed="false"` and no `bg-blue-100` class

Follow the existing Toolbar test patterns for rendering and assertions.
  </action>
  <verify>
Run `cd /Users/Daniel.Tehan/Code/lineage/lineage-ui && npm test -- --run` to verify all tests pass (existing 260+ plus new tests). Run `cd /Users/Daniel.Tehan/Code/lineage/lineage-ui && npm test -- --run useMultiSelect` to verify the new hook tests specifically.
  </verify>
  <done>
useMultiSelect.test.ts has 6 passing tests. Toolbar.test.tsx has 5 new passing tests for multi-select toggle. All 260+ existing tests continue to pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify multi-select and group move in browser</name>
  <files>lineage-ui/src/components/domain/LineageGraph/LineageGraph.tsx, lineage-ui/src/components/domain/LineageGraph/TableNode/TableNode.tsx, lineage-ui/src/components/domain/LineageGraph/Toolbar.tsx</files>
  <action>
Human verification checkpoint. What was built: Multi-select and group move for lineage graph table nodes using React Flow's built-in multi-selection. Cmd+click to add nodes to selection, drag to move group, toolbar toggle button for mode, Escape to exit.

Steps to verify:
1. Start the frontend dev server: `cd lineage-ui && npm run dev`
2. Navigate to any lineage graph with multiple table nodes
3. **Cmd+click multi-select:** Hold Cmd (Mac) or Ctrl (Windows/Linux) and click 2-3 different table nodes. Verify:
   - Each clicked node gets a blue ring outline (ring-blue-400)
   - No column selection or path highlighting fires
   - No detail panel opens
4. **Group drag:** While multiple nodes are selected, drag one of them. Verify all selected nodes move together as a group.
5. **Toolbar toggle:** Click the multi-select toggle button in the toolbar (cursor-click icon). Verify:
   - Button shows active state (blue background)
   - Clicking any node adds it to selection WITHOUT holding Cmd
   - Clicking another node adds it (does not replace)
6. **Escape exit:** Press Escape. Verify:
   - Multi-select mode deactivates (toolbar button returns to inactive)
   - All node selection rings disappear
7. **Path highlight preserved:** With multi-select OFF, click a column row. Verify path highlighting and detail panel still work normally.
8. **Mode switch clears highlight:** With a column selected and path highlighted, click the toolbar multi-select toggle. Verify the path highlight and dimming clear immediately.

Resume signal: Type "approved" or describe issues
  </action>
  <verify>Human confirms Cmd+click multi-select, group drag, toolbar toggle, Escape exit, and path highlight preservation all work correctly</verify>
  <done>User has approved the multi-select and group move interactive behavior</done>
</task>

</tasks>

<verification>
1. `cd /Users/Daniel.Tehan/Code/lineage/lineage-ui && npm test -- --run` — All tests pass (260+ existing + ~11 new)
2. `cd /Users/Daniel.Tehan/Code/lineage/lineage-ui && npx tsc --noEmit` — TypeScript compiles cleanly
3. Human verifies interactive multi-select, group drag, toolbar toggle, and Escape exit work in browser
</verification>

<success_criteria>
- All unit tests pass including new useMultiSelect and Toolbar multi-select tests
- Human verifies:
  - Cmd+click adds nodes to selection with blue ring visual
  - Group drag moves all selected nodes together
  - Toolbar toggle enables click-to-select without modifier key
  - Escape exits multi-select mode and clears selection
  - Existing column selection and path highlighting unaffected when multi-select inactive
</success_criteria>

<output>
After completion, create `.planning/phases/13-multi-select-group-move-in-lineage-graph/13-02-SUMMARY.md`
</output>
